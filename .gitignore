# Dependencies (restored by `npm install`)
node_modules/

# OS / tooling noise
.DS_Store

# Logs and temp
*.log
npm-debug.log*

# Local clasp binding (scriptId is project-specific; use .clasp.json.example + copy)
.clasp.json

# Local deploy helper with your deployment ID (use pushNdeploy.example.sh as template)
pushNdeploy.sh

# Optional: env files with secrets
.env
.env.local
.env.*.local
